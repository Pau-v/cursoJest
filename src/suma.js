// npm init para iniciar proyecto

// npm install jest –D para instalar jest en modo desarrollador

// en scripts en packaje.json especificamos q usaremos jest

// carpeta __test__ por convención, dentro de ella creamos un archivo en el que se harán las pruebas

// los archivos de  __test__ serán nombre.test.js


function suma(a, b){
    return a + b
}

module.exports = suma